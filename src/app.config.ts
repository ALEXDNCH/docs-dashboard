// Если хотим добавить задержку перед получением даты, для показа прелоадера, 0 выключаем задержку
export const DELAY_FOR_QUERY = 1000;

// Хотим показать ошибку при запросе?
export const IS_ERROR_FOR_QUERY = false;
